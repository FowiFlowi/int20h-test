import { SearchProductsResponse, ShopProvider } from '@interfaces/service/provider/shop'
import { ZakazResponse } from '@interfaces/service/provider/shop/zakaz'

// class ZakazShopProvider implements ShopProvider {
//     async searchProducts(query: string): Promise<SearchProductsResponse> {

//     }

//     private async makeApiCall(query: string): Promise<ZakazResponse> {

//     }
// }

// export default new ZakazShopProvider()
